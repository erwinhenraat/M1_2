using UnityEngine;

public class Demo : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Goedemorgen");
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("Goedenavond");
    }
}
